<h1>This is my Index</h1>

<% @tasks.each do |task| %>
  <%= link_to task.title, task_path(task) %>
  <%= link_to "Delete task", task_path(task.id), class: 'delete', method: :delete, data: {confirm: "Are you sure?"} %>
  <hr>
<% end %>


<%= link_to "Add a task", new_task_path %>
